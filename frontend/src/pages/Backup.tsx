/**
 * Backup page - Backup management interface
 * Displays the BackupManager component
 */

import { BackupManager } from '../components/backup/BackupManager';

export default function Backup() {
  return <BackupManager />;
}